# bazzite-niri

This is a custom bazzite image which is based on bazzite-gnome. It adds niri as an option in gdm and includes things like waybar, fuzzel, alacritty, etc (check the build.sh for the fairly short list of additions).

